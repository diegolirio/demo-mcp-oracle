{
  "mcpServers": {
    "{server-name}": {
      "command": "uvx",
      "args": [
        "--from",
        "{mcp-package}",
        "{mcp-entrypoint}"
      ],
      "env": {
        "DB_CONNECTION_STRING": "${ENV_VAR_NAME}"
      }
    }
  }
}
